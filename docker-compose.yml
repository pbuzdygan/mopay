services:
  app:
    build: .
    container_name: Mopay
    restart: unless-stopped
    environment:
      - PORT=8010
      - DB_FILE=/data/mopay.sqlite
      - APP_PIN=123456
      - NODE_ENV=production
    volumes:
      - ./data:/data
    ports:
      - "8010:8010"
